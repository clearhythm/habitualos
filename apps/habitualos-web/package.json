{
  "name": "habitualos-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "eleventy:build": "eleventy",
    "eleventy:serve": "eleventy --serve --port=8080",
    "start": "eleventy --serve --port=8080",
    "build": "eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "sass": "^1.69.5"
  }
}
