<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title or "Signal" }}</title>
  <meta name="description" content="{{ description or 'Signal' }}">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="icon" type="image/x-icon" href="/assets/favicon.ico">
  <link rel="icon" type="image/png" sizes="32x32" href="/assets/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="16x16" href="/assets/favicon-16x16.png">
  <link rel="apple-touch-icon" sizes="180x180" href="/assets/apple-touch-icon.png">
  <link rel="stylesheet" href="/styles/main.css">
  {% if css %}<link rel="stylesheet" href="{{ css }}">{% endif %}
  <script>
    // userId must be available before any module scripts run
    window.__userId = localStorage.getItem('userId') || (function() {
      var id = 'u-' + Date.now() + '-' + Math.random().toString(36).slice(2, 8);
      localStorage.setItem('userId', id);
      return id;
    })();
  </script>
</head>
<body{% if bodyClass %} class="{{ bodyClass }}"{% endif %}>

  <nav class="navbar">
    <div class="sidemenu">
      {% include "nav.njk" %}
      <div class="sidemenu-main" id="sidemenu-main">
        <div class="sidemenu-left">
          <ul id="nav-marketing-links">
            <li><a href="/">Welcome</a></li>
            <li><a href="/#what">What Signal Does</a></li>
            <li><a href="/#how">How Signal Works</a></li>
            <li><a href="/#fit-score">The Fit Score</a></li>
            <li><a href="/early-access/">Early Access</a></li>
          </ul>
          <ul id="nav-app-links" hidden>
            <li><a href="/dashboard/">Dashboard</a></li>
            <li><a href="/profile/">Profile</a></li>
          </ul>

          <div class="sidemenu-auth">
            <div id="nav-signed-out" class="sidemenu-auth-state" hidden>
              <a href="/signin/" class="sidemenu-auth-link">Sign in</a>
            </div>
            <div id="nav-signed-in" class="sidemenu-auth-state" hidden>
              <span class="sidemenu-auth-avatar" id="nav-auth-avatar" aria-hidden="true"></span>
              <span class="sidemenu-auth-name" id="nav-auth-name"></span>
              <button class="sidemenu-auth-link" id="nav-signout-btn">Sign out</button>
            </div>
          </div>

          <div class="sidemenu-footer-row">
            <img src="/assets/favicon-32x32.png" alt="" class="sidemenu-logo">
            <p class="sidemenu-copyright"><strong>Signal</strong>. Powered by real work, not r&eacute;sum&eacute;s.</p>
          </div>
        </div>
        <div class="sidemenu-right">
          <div class="sidemenu-overlay" id="sidemenu-overlay"></div>
        </div>
      </div>
    </div>
    <a class="navbar-brand" href="/"><img src="/assets/favicon-32x32.png" alt="" class="navbar-brand-icon" aria-hidden="true">{{ pageTitle or "Signal" }}</a>
  </nav>

  <main class="main">
    {{ content | safe }}
  </main>

  {% if showDemoModal %}
  {% from "modal.njk" import widget %}
  {{ widget() }}
  {% endif %}

  <footer class="site-footer">
    <div class="container">
      <a href="/early-access/" class="footer-logo-link"><img src="/assets/favicon-32x32.png" alt="" class="footer-logo"></a>
      <p>An <a href="https://erikburns.com">Erik</a> &amp; <a href="https://claude.ai">Claude</a> app © 2026 All rights reserved.<br/>
      Signal was built with <a href="https://habitualos.com">HabitualOS</a></p>
    </div>
  </footer>

  <script src="/assets/js/navigation.js"></script>
  {% if pageScript %}
  <script type="module" src="{{ pageScript }}"></script>
  {% endif %}

  <script>
    // Auth nav state — runs after body so elements exist, no DOMContentLoaded needed
    (function() {
      var signedIn = !!localStorage.getItem('signal-owner-id');
      var signedOutEl = document.getElementById('nav-signed-out');
      var signedInEl  = document.getElementById('nav-signed-in');

      if (signedIn) {
        if (signedInEl) signedInEl.removeAttribute('hidden');

        var ownerName = localStorage.getItem('signal-owner-name') || '';
        var nameEl = document.getElementById('nav-auth-name');
        if (nameEl) nameEl.textContent = ownerName;
        var avatarEl = document.getElementById('nav-auth-avatar');
        if (avatarEl) avatarEl.textContent = ownerName.charAt(0).toUpperCase();

        var marketingLinks = document.getElementById('nav-marketing-links');
        if (marketingLinks) marketingLinks.setAttribute('hidden', '');
        var appLinks = document.getElementById('nav-app-links');
        if (appLinks) appLinks.removeAttribute('hidden');

        var signOutBtn = document.getElementById('nav-signout-btn');
        if (signOutBtn) {
          signOutBtn.addEventListener('click', function() {
            localStorage.removeItem('signal-owner-id');
            localStorage.removeItem('signal-owner-name');
            window.location.href = '/';
          });
        }
      } else {
        if (signedOutEl) signedOutEl.removeAttribute('hidden');
      }
    })();
  </script>
</body>
</html>
