# =============================================================================
# Local Development Configuration
# =============================================================================
# This file is used for local netlify dev only.
# Production deploys use the root-level netlify.toml.

[dev]
  command = "npm run dev"
  targetPort = 8080
  autoLaunch = false
