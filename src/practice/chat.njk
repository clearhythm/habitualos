---
title: Get Motivated - HabitualOS
layout: base.njk
permalink: /practice/chat/
---

<style>
  /* Full-height chat layout - only for this page */
  body {
    overflow: hidden; /* Prevent body scroll */
  }

  footer {
    display: none !important; /* Hide footer on chat page */
  }

  /* Ensure header is transparent */
  body > nav {
    background: transparent !important;
  }

  #chat-page-container {
    overflow: hidden;
  }
</style>

<!-- Chat Container -->
<div id="chat-page-container" style="position: fixed; top: 60px; left: 0; right: 0; bottom: 0; display: flex; flex-direction: column;">
  <div id="chat-container" style="display: flex; flex-direction: column; height: 100%;">
    <!-- Messages (scrollable area) - scrollbar at viewport edge -->
    <div id="chat-messages" class="chat-messages" style="flex: 1; min-height: 0; overflow-y: auto; width: 100%;">
      <div id="chat-messages-inner" style="max-width: 700px; margin: 0 auto; padding: 1.5rem; display: flex; flex-direction: column; gap: 1rem;"></div>
    </div>

    <!-- Input area (fixed at bottom) -->
    <div style="flex-shrink: 0; flex-grow: 0; padding: 1rem 1.5rem; display: flex; justify-content: center;">
      <div style="width: 100%; max-width: 700px;">
      <form id="chat-form">
        <div style="position: relative;">
          <textarea
            name="message"
            id="message-input"
            placeholder="Reply..."
            required
            rows="2"
            style="width: 100%; padding: 0.75rem 3rem 0.75rem 0.75rem; border: 1px solid #ddd; border-radius: 20px; font-size: 1rem; font-family: inherit; resize: none; max-height: 150px; overflow-y: auto; box-sizing: border-box;"
          ></textarea>
          <button
            type="submit"
            id="send-button"
            disabled
            style="position: absolute; right: 0.5rem; bottom: 0.5rem; width: 32px; height: 32px; background: #ddd; color: white; border: none; border-radius: 50%; cursor: not-allowed; transition: all 0.2s; display: flex; align-items: center; justify-content: center; padding: 0;"
          >
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 256 256">
              <path d="M208.49,120.49a12,12,0,0,1-17,0L140,69V216a12,12,0,0,1-24,0V69L64.49,120.49a12,12,0,0,1-17-17l72-72a12,12,0,0,1,17,0l72,72A12,12,0,0,1,208.49,120.49Z"></path>
            </svg>
          </button>
        </div>
      </form>

      <!-- Actions below input -->
      <div style="display: flex; justify-content: center; gap: 1.5rem; padding-top: 0.75rem;">
        <button
          id="save-chat-btn"
          type="button"
          style="background: none; border: none; color: #9b59b6; font-size: 0.85rem; cursor: pointer; padding: 0.25rem 0.5rem; transition: color 0.2s;"
          onmouseover="this.style.color='#8b4a96'"
          onmouseout="this.style.color='#9b59b6'"
          title="Save conversation"
        >
          Save
        </button>
        <button
          id="reset-chat-btn"
          type="button"
          style="background: none; border: none; color: #999; font-size: 0.85rem; cursor: pointer; padding: 0.25rem 0.5rem; transition: color 0.2s;"
          onmouseover="this.style.color='#666'"
          onmouseout="this.style.color='#999'"
          title="Start over"
        >
          Reset
        </button>
      </div>
      </div>
    </div>

    <!-- "I'm Ready" Modal Overlay (appears after READY_TO_PRACTICE signal) -->
    <div id="ready-button-container" style="display: none; position: fixed; top: 0; left: 0; right: 0; bottom: 0; background: rgba(0, 0, 0, 0.5); z-index: 1000; align-items: center; justify-content: center;">
      <div style="background: white; padding: 3rem 2rem; border-radius: 12px; text-align: center; max-width: 400px; margin: 0 1rem; box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);">
        <button
          id="ready-button"
          style="padding: 1rem 2rem; background: #9b59b6; color: white; border: none; border-radius: 24px; font-size: 1rem; font-weight: 600; cursor: pointer; transition: all 0.2s; box-shadow: 0 2px 8px rgba(155, 89, 182, 0.3);"
          onmouseover="this.style.background='#8b4a96'; this.style.boxShadow='0 4px 12px rgba(155, 89, 182, 0.4)'"
          onmouseout="this.style.background='#9b59b6'; this.style.boxShadow='0 2px 8px rgba(155, 89, 182, 0.3)'"
        >
          I'm Ready to Practice
        </button>
        <button
          id="dismiss-ready-button"
          style="display: block; margin: 1.5rem auto 0; padding: 0.5rem 1rem; background: transparent; color: #999; border: none; font-size: 0.9rem; cursor: pointer; text-decoration: underline;"
          onmouseover="this.style.color='#666'"
          onmouseout="this.style.color='#999'"
        >
          Not yet, keep talking
        </button>
      </div>
    </div>
  </div>

  </div>

  <!-- "Do Your Practice" Overlay -->
  <div id="do-practice-overlay" style="display: none; position: fixed; top: 0; left: 0; right: 0; bottom: 0; background: white; z-index: 2000; align-items: center; justify-content: center; flex-direction: column; padding: 2rem;">
    <div style="text-align: center; max-width: 400px;">
      <h2 style="font-size: 2rem; margin-bottom: 1rem; color: #333;">Do Your Practice</h2>
      <p style="font-size: 1.1rem; color: #666; margin-bottom: 2rem;">
        Return here when you've completed it to track that you did it. You got this.
      </p>
      <button
        id="i-practiced-button"
        style="padding: 1rem 2rem; background: #0066cc; color: white; border: none; border-radius: 4px; font-size: 1rem; font-weight: 600; cursor: pointer; transition: background 0.2s;"
        onmouseover="this.style.background='#0052a3'"
        onmouseout="this.style.background='#0066cc'"
      >
        I Practiced
      </button>
    </div>
  </div>
</div>

<script type="module">
  import { saveChatHistory, getChatHistory, clearChatHistory, savePracticeChatState, getPracticeChatState, clearPracticeChatState, saveChatId, getChatId, saveLastSavedIndex, getLastSavedIndex } from '/assets/js/practice-chat-state.js';
  import { initializeUser, getUserId } from '/assets/js/auth/auth.js';
  import { showToast } from '/assets/js/components/chat-toast.js';

  // Initialize user and get userId
  initializeUser();
  const userId = getUserId();

  const chatMessages = document.getElementById('chat-messages-inner');
  const chatMessagesContainer = document.getElementById('chat-messages');
  const chatForm = document.getElementById('chat-form');
  const messageInput = document.getElementById('message-input');
  const sendButton = document.getElementById('send-button');
  const readyButtonContainer = document.getElementById('ready-button-container');
  const readyButton = document.getElementById('ready-button');
  const dismissReadyButton = document.getElementById('dismiss-ready-button');
  const chatContainer = document.getElementById('chat-container');
  const doPracticeOverlay = document.getElementById('do-practice-overlay');
  const iPracticedButton = document.getElementById('i-practiced-button');

  // Track practice data when ready
  let currentPracticeData = null;
  let loadingMessageElement = null;

  // Streaming state
  let streamingMessageElement = null;
  let streamingText = '';
  let streamingStarted = false;

  // Update send button state based on textarea content
  function updateSendButton() {
    const hasContent = messageInput.value.trim().length > 0;
    sendButton.disabled = !hasContent;

    if (hasContent) {
      sendButton.style.background = '#9b59b6';
      sendButton.style.cursor = 'pointer';
    } else {
      sendButton.style.background = '#ddd';
      sendButton.style.cursor = 'not-allowed';
    }
  }

  // Show loading message inline
  function showLoadingMessage() {
    const loadingDiv = document.createElement('div');
    loadingDiv.style.cssText = 'padding: 0.875rem 1rem 1rem 1rem; border-radius: 8px; max-width: 80%; word-wrap: break-word; line-height: 1.5; background-color: #f9f6ff; color: #333; align-self: flex-start; border-left: 3px solid #ddd; border-bottom-left-radius: 4px;';
    loadingDiv.innerHTML = '<span style="color: #999; font-style: italic;">Obi-Wai is thinking...</span>';
    chatMessages.appendChild(loadingDiv);
    chatMessagesContainer.scrollTop = chatMessagesContainer.scrollHeight;
    loadingMessageElement = loadingDiv;
  }

  // Hide loading message
  function hideLoadingMessage() {
    if (loadingMessageElement) {
      loadingMessageElement.remove();
      loadingMessageElement = null;
    }
  }

  // Streaming message helpers
  function showStreamingMessage() {
    streamingText = '';
    streamingStarted = false;
    const messageDiv = document.createElement('div');
    messageDiv.style.cssText = 'padding: 0.875rem 1rem 1rem 1rem; border-radius: 8px; max-width: 80%; word-wrap: break-word; line-height: 1.5; background-color: #f9f6ff; color: #333; align-self: flex-start; border-left: 3px solid #ddd; border-bottom-left-radius: 4px;';
    messageDiv.innerHTML = '<span style="color: #999; font-style: italic;">Obi-Wai is thinking...</span>';
    chatMessages.appendChild(messageDiv);
    chatMessagesContainer.scrollTop = chatMessagesContainer.scrollHeight;
    streamingMessageElement = messageDiv;

    // Add cursor blink animation if not present
    if (!document.getElementById('streaming-cursor-style')) {
      const style = document.createElement('style');
      style.id = 'streaming-cursor-style';
      style.textContent = '@keyframes blink { 0%, 50% { opacity: 1; } 51%, 100% { opacity: 0; } }';
      document.head.appendChild(style);
    }
  }

  function appendStreamingText(text) {
    if (!streamingMessageElement) return;
    streamingText += text;

    // On first token, switch from "Thinking..." to streaming mode
    if (!streamingStarted) {
      streamingStarted = true;
      streamingMessageElement.style.borderLeftColor = '#9b59b6';
    }

    // Update content with cursor at end
    if (window.marked) {
      streamingMessageElement.innerHTML = marked.parse(streamingText) + '<span class="streaming-cursor" style="display: inline-block; width: 2px; height: 1em; background: #9b59b6; animation: blink 1s infinite;"></span>';
    } else {
      streamingMessageElement.textContent = streamingText;
    }
    chatMessagesContainer.scrollTop = chatMessagesContainer.scrollHeight;
  }

  function finalizeStreamingMessage() {
    if (!streamingMessageElement) return { text: '', displayText: '' };

    // Remove cursor and finalize content
    const cursor = streamingMessageElement.querySelector('.streaming-cursor');
    if (cursor) cursor.remove();

    // Re-render with final markdown
    if (window.marked && streamingText) {
      streamingMessageElement.innerHTML = marked.parse(streamingText);
    } else if (streamingText) {
      streamingMessageElement.textContent = streamingText;
    }

    const finalText = streamingText;
    streamingMessageElement = null;
    streamingText = '';
    streamingStarted = false;

    return { text: finalText, displayText: finalText };
  }

  function hideStreamingMessage() {
    if (streamingMessageElement) {
      streamingMessageElement.remove();
      streamingMessageElement = null;
      streamingText = '';
      streamingStarted = false;
    }
  }

  // Parse READY_TO_PRACTICE signal from response
  function parseReadySignal(text) {
    if (!text.includes('READY_TO_PRACTICE')) return null;

    const lines = text.split('\n');
    let practiceName = '';
    let message = '';
    let inMessage = false;

    for (let i = 0; i < lines.length; i++) {
      const line = lines[i];
      if (line.startsWith('PRACTICE_NAME:')) {
        practiceName = line.substring(14).trim();
      } else if (line.startsWith('MESSAGE:')) {
        message = line.substring(8).trim();
        inMessage = true;
      } else if (inMessage && line.trim() && !line.startsWith('READY_TO_PRACTICE') && !line.startsWith('---')) {
        message += ' ' + line.trim();
      }
    }

    if (practiceName) {
      return { practiceName: practiceName.trim(), message: message.trim() };
    }
    return null;
  }

  // Get display text (without signal content)
  function getDisplayText(text) {
    if (!text.includes('READY_TO_PRACTICE')) return text;
    const signalIndex = text.indexOf('READY_TO_PRACTICE');
    return text.substring(0, signalIndex).trim();
  }

  // Load chat history from localStorage
  let chatHistory = getChatHistory();

  // Render a message bubble
  function renderMessage(role, content) {
    const messageDiv = document.createElement('div');
    messageDiv.style.cssText = 'padding: 0.875rem 1rem 1rem 1rem; border-radius: 8px; max-width: 80%; word-wrap: break-word; line-height: 1.5;';

    if (role === 'assistant') {
      // Obi-Wai message - purple theme, left-aligned
      messageDiv.style.cssText += 'background-color: #f9f6ff; color: #333; align-self: flex-start; border-left: 3px solid #9b59b6; border-bottom-left-radius: 4px;';

      // Render markdown for assistant messages
      if (window.marked) {
        messageDiv.innerHTML = marked.parse(content);
      } else {
        messageDiv.textContent = content;
      }
    } else {
      // User message - blue theme, right-aligned
      messageDiv.style.cssText += 'background-color: #0066cc; color: white; align-self: flex-end; border-bottom-right-radius: 4px;';
      messageDiv.textContent = content;
    }

    chatMessages.appendChild(messageDiv);
    chatMessagesContainer.scrollTop = chatMessagesContainer.scrollHeight;
  }

  // Initialize chat with opening message or load history
  if (chatHistory.length === 0) {
    const openingMessage = {
      role: 'assistant',
      content: "What would you like to practice today?",
      timestamp: new Date().toISOString()
    };
    chatHistory.push(openingMessage);
    saveChatHistory(chatHistory);
  }

  // Add header to chat messages area
  const headerDiv = document.createElement('div');
  headerDiv.style.cssText = 'text-align: center; padding: 2rem 1rem 1rem 1rem; margin-bottom: 1rem;';
  headerDiv.innerHTML = `
    <div style="font-size: 3rem; margin-bottom: .5rem;">🐉</div>
    <h1 style="margin: 0 0 0.5rem 0; font-size: 2rem; color: #333; font-weight: 600;">Motivate</h1>
    <p style="margin: 0; font-size: 0.95rem; color: #999;">Obi-Wai can help light up your path.</p>
  `;
  chatMessages.appendChild(headerDiv);

  // Render all messages from history
  chatHistory.forEach(msg => {
    renderMessage(msg.role, msg.content);
  });

  // Streaming chat handler
  async function handleStreamingChat(message) {
    showStreamingMessage();

    const userTimezone = Intl.DateTimeFormat().resolvedOptions().timeZone;

    const response = await fetch('/api/chat-stream', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        chatType: 'obi-wai',
        userId,
        message,
        timezone: userTimezone,
        chatHistory: chatHistory.slice(0, -1).map(msg => ({
          role: msg.role,
          content: msg.content
        }))
      })
    });

    if (!response.ok) {
      throw new Error(`Stream request failed: ${response.status}`);
    }

    const reader = response.body.getReader();
    const decoder = new TextDecoder();
    let buffer = '';
    let signalData = null;

    while (true) {
      const { done, value } = await reader.read();
      if (done) break;

      buffer += decoder.decode(value, { stream: true });

      // Parse SSE lines from buffer
      const lines = buffer.split('\n\n');
      buffer = lines.pop() || '';

      for (const line of lines) {
        if (line.startsWith('data: ')) {
          try {
            const data = JSON.parse(line.slice(6));

            if (data.type === 'token') {
              appendStreamingText(data.text);
            } else if (data.type === 'done') {
              // Check for signal in the done event
              if (data.hasSignal && data.signal) {
                signalData = data.signal;
              }
            } else if (data.type === 'error') {
              throw new Error(data.error);
            }
          } catch (parseErr) {
            console.warn('Failed to parse SSE data:', parseErr);
          }
        }
      }
    }

    // Finalize the streaming message
    const { text: fullResponse } = finalizeStreamingMessage();

    // Parse signal from the full response (backup check)
    const parsedSignal = parseReadySignal(fullResponse);

    // Get display text (without signal content)
    const displayText = getDisplayText(fullResponse);

    // Update the rendered message to show only display text
    if (displayText !== fullResponse && streamingMessageElement === null) {
      // Find the last message and update it
      const lastMessage = chatMessages.lastElementChild;
      if (lastMessage && window.marked) {
        lastMessage.innerHTML = marked.parse(displayText);
      }
    }

    // Add to chat history (use display text)
    const assistantMessage = {
      role: 'assistant',
      content: displayText || fullResponse,
      timestamp: new Date().toISOString()
    };
    chatHistory.push(assistantMessage);
    saveChatHistory(chatHistory);

    // Handle ready to practice signal
    if (parsedSignal || (signalData?.type === 'READY_TO_PRACTICE')) {
      const readyData = parsedSignal || signalData.data;
      currentPracticeData = {
        practiceName: readyData.practiceName || readyData.PRACTICE_NAME,
        fullSuggestion: displayText
      };

      // Show "I'm Ready" modal
      readyButtonContainer.style.display = 'flex';

      // Disable chat form
      sendButton.disabled = true;
      sendButton.style.background = '#ddd';
      sendButton.style.cursor = 'not-allowed';
      messageInput.disabled = true;
    }
  }

  // Non-streaming fallback
  async function handleNonStreamingChat(message) {
    showLoadingMessage();

    const userTimezone = Intl.DateTimeFormat().resolvedOptions().timeZone;

    const response = await fetch('/.netlify/functions/practice-chat', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        userId,
        message,
        timezone: userTimezone,
        chatHistory: chatHistory.map(msg => ({
          role: msg.role,
          content: msg.content
        }))
      })
    });

    const data = await response.json();

    if (!data.success) {
      throw new Error(data.error || 'Failed to get response');
    }

    hideLoadingMessage();

    const assistantMessage = {
      role: 'assistant',
      content: data.response,
      timestamp: new Date().toISOString()
    };
    renderMessage('assistant', data.response);
    chatHistory.push(assistantMessage);
    saveChatHistory(chatHistory);

    // Check if ready to practice
    if (data.ready) {
      currentPracticeData = {
        practiceName: data.practiceName,
        fullSuggestion: data.response
      };

      // Show "I'm Ready" modal
      readyButtonContainer.style.display = 'flex';

      // Disable chat form
      sendButton.disabled = true;
      sendButton.style.background = '#ddd';
      sendButton.style.cursor = 'not-allowed';
      messageInput.disabled = true;
    }
  }

  // Handle form submission
  chatForm.addEventListener('submit', async (e) => {
    e.preventDefault();

    const message = messageInput.value.trim();
    if (!message) return;

    // Add user message to UI and history
    const userMessage = {
      role: 'user',
      content: message,
      timestamp: new Date().toISOString()
    };
    renderMessage('user', message);
    chatHistory.push(userMessage);
    saveChatHistory(chatHistory);

    // Clear input
    messageInput.value = '';
    messageInput.style.height = 'auto';

    // Disable send button
    sendButton.disabled = true;
    sendButton.style.background = '#ddd';
    sendButton.style.cursor = 'not-allowed';

    // Try streaming first, fall back to non-streaming on error
    try {
      await handleStreamingChat(message);
    } catch (streamError) {
      console.warn('Streaming failed, falling back to regular endpoint:', streamError);
      hideStreamingMessage();
      try {
        await handleNonStreamingChat(message);
      } catch (error) {
        console.error('Error sending message:', error);
        hideLoadingMessage();

        // Show error message
        const errorMessage = {
          role: 'assistant',
          content: "I'm having trouble connecting right now. Could you try again?",
          timestamp: new Date().toISOString()
        };
        renderMessage('assistant', errorMessage.content);
        chatHistory.push(errorMessage);
        saveChatHistory(chatHistory);
      }
    }
  });

  // Handle "I'm Ready" button
  readyButton.addEventListener('click', () => {
    if (!currentPracticeData) return;

    // Save practice suggestion to localStorage
    savePracticeChatState({
      suggestedPractice: currentPracticeData.practiceName,
      fullSuggestion: currentPracticeData.fullSuggestion,
      timestamp: new Date().toISOString()
    });

    // Show "Do Your Practice" screen
    chatContainer.style.display = 'none';
    doPracticeOverlay.style.display = 'flex';
  });

  // Handle dismiss "I'm Ready" button
  dismissReadyButton.addEventListener('click', () => {
    // Hide ready button, re-enable chat
    readyButtonContainer.style.display = 'none';
    messageInput.disabled = false;
    messageInput.focus();
    updateSendButton(); // Update button state based on current textarea content
  });

  // Handle "I Practiced" button
  iPracticedButton.addEventListener('click', () => {
    // Navigate to practice form
    window.location.href = '/practice/log';
  });

  // Handle Enter key - auto-submit on desktop only, newline on mobile
  messageInput.addEventListener('keydown', function(e) {
    if (e.key === 'Enter' && !e.shiftKey) {
      const isMobile = window.matchMedia('(pointer: coarse)').matches;
      if (!isMobile) {
        e.preventDefault();
        chatForm.requestSubmit();
      }
    }
  });

  // Auto-resize textarea and toggle send button
  messageInput.addEventListener('input', function() {
    this.style.height = 'auto';
    this.style.height = (this.scrollHeight) + 'px';
    updateSendButton();
  });

  // Handle save chat button
  document.getElementById('save-chat-btn').addEventListener('click', async () => {
    if (chatHistory.length === 0) {
      showToast('No conversation to save.', { type: 'info' });
      return;
    }

    const saveBtn = document.getElementById('save-chat-btn');
    const originalText = saveBtn.innerHTML;

    // Determine which messages are new (not yet saved)
    const lastSavedIndex = getLastSavedIndex();
    const newMessages = chatHistory.slice(lastSavedIndex);

    if (newMessages.length === 0) {
      showToast('Already saved.', { type: 'info' });
      return;
    }

    const existingChatId = getChatId();
    const isAppend = existingChatId && lastSavedIndex > 0;

    try {
      // Show loading state
      saveBtn.textContent = 'Saving...';
      saveBtn.disabled = true;

      // Get current practice chat state (if any)
      const chatState = getPracticeChatState();

      // Save practice chat to backend (only new messages for append)
      const response = await fetch('/.netlify/functions/practice-chat-save', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        },
        body: JSON.stringify({
          userId,
          messages: isAppend ? newMessages : chatHistory,
          suggestedPractice: chatState?.suggestedPractice || null,
          fullSuggestion: chatState?.fullSuggestion || null,
          completed: chatState?.suggestedPractice ? true : false,
          chatId: existingChatId,
          mode: isAppend ? 'append' : 'create'
        })
      });

      const data = await response.json();

      if (!data.success) {
        throw new Error(data.error || 'Failed to save practice chat');
      }

      // Track the chat ID for future appends
      if (!existingChatId && data.chatId) {
        saveChatId(data.chatId);
      }

      // Update last saved index to current length
      saveLastSavedIndex(chatHistory.length);

      // Restore button and show success
      saveBtn.innerHTML = originalText;
      saveBtn.disabled = false;
      showToast('Saved!');

    } catch (error) {
      console.error('Error saving practice chat:', error);
      showToast('Failed to save. Please try again.', { type: 'error' });

      // Restore button state
      saveBtn.innerHTML = originalText;
      saveBtn.disabled = false;
    }
  });

  // Handle reset chat button
  document.getElementById('reset-chat-btn').addEventListener('click', () => {
    if (confirm('Start a fresh conversation? This will clear your current chat.')) {
      clearChatHistory();
      window.location.reload();
    }
  });

  // Focus message input on load
  messageInput.focus();
</script>
